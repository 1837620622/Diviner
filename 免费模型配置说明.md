# 推演线路说明（模型选择器架构）

前台只请求本站 `/api/chat`，并在请求体里携带所选模型的 `model` id。
浏览器端只见厂商图标与模型代号，看不到上游网关与真实模型代号，也不下发任何密钥。

## 运行方式

问卜者在输入框上方的「法器（模型）选择器」中自主择器。所选 id 由
`functions/api/chat.js` 的路由表映射到对应上游线路，单线路直连，
**不再做跨模型静默兜底**。某条线路失败时，服务端回一个带
`model_error` 标记的 SSE 帧，前端提示「另择一尊法器」并自动展开选择器。

## 可选模型与上游（仅供配置者参考，前台不可见）

| 前台代号 | 上游 | 读图 | 备注 |
| --- | --- | --- | --- |
| 通义千问 3.6（默认首选） | Groq | 是 | 速度快，图文兼通 |
| DeepSeek V4 视觉版 | b.ai 聚合 | 是 | 深度推理 + 读图 |
| DeepSeek V4 Flash | b.ai 聚合 | 否 | 推理链缜密 |
| 智谱清言 GLM-5.3 | b.ai 聚合 | 否 | 中文醇厚 |
| 腾讯混元 HY3 | b.ai 聚合 | 否 | 国学底蕴 |
| 智谱清言 GLM-4.7 | 智谱开放平台 | 否 | 直连专线，出语快 |
| 小米 MiMo 2.5 | b.ai 聚合 | 否 | 思路清晰 |
| 通义千问 3.8 Flash | b.ai 聚合 | 否 | 极速短答 |

> b.ai 聚合网关一个 Key 覆盖 DeepSeek / 千问 / 智谱 / 混元 / 小米等免费模型。
> 推理类模型（DeepSeek、混元、GLM-5.3、MiMo、千问 3.8）会先输出
> `reasoning_content`，服务端只透传 `content`，思考链不会泄漏给前台；
> 故这些线路的生成上限已抬高，避免思考耗尽 token 导致正文为空。

## Cloudflare 需配置的变量与 Secrets

全部在 Cloudflare Dashboard → Pages 项目 → Settings → Variables and Secrets 设置，
**绝不写进仓库**（`.dev.vars` 仅本地用，已被 git 忽略）：

- `B_AI_API_KEY`：b.ai 聚合网关密钥（新增，供上表多数模型使用）
- `GROQ_API_KEY`：Groq 密钥（供默认首选通义千问 3.6）
- `ZHIPU_API_KEY`：智谱开放平台密钥（供 GLM-4.7）
- `ADMIN_PASSWORD`：后台口令（既有）

绑定：`[ai] binding = "AI"`（`wrangler.toml` 已配置）；KV 命名空间 `CHAT_LOGS` 用于留档。

可选：`GROQ_MODEL`、`ZHIPU_MODEL` 可覆盖默认真实模型代号；未设置时用内置默认值。

## 本地调试

把密钥写进项目根目录 `.dev.vars`（勿提交），然后：

```
npx wrangler pages dev . --ai AI
```

## 部署

```
npx wrangler pages deploy . --project-name=diviner --commit-dirty=true
```
