# 玄机子 v6 免费模型与密钥配置说明

本版本把 `/api/chat` 做成“供应商级自动容灾”，并额外接入一个**无需 API Key** 的公共 Qwen3.8-Flash-Next 端点。任一线路遇到 429、超时、额度耗尽或返回空内容时，会自动继续尝试下一条线路。

## 建议的稳定顺序

默认：`Groq → 智谱 → Cloudflare Workers AI → HF 公共 Qwen → Gemini → 自定义旧接口`

- **Groq**：主要承担文字与图片问卜；官方 Free Plan，需 `GROQ_API_KEY`。
- **智谱 GLM-4-Flash-250414**：中文文本免费兜底；需 `ZHIPU_API_KEY`。
- **Cloudflare Workers AI**：如果站点本身部署在 Cloudflare，只需绑定 `AI`，无需额外第三方 API Key。
- **HF 公共 Qwen3.8-Flash-Next**：真正无需 Key、无需注册，但属于社区共享临时端点，会限流，也明确可能退役。因此只放在稳定官方线路之后。
- **Gemini**：可选视觉备用；配置 `GEMINI_API_KEY` 后自动启用。

## 你现在需要在 Cloudflare 里设置的变量

请在 `Workers & Pages → 你的项目 → Settings → Variables and Secrets` 中添加：

- `GROQ_API_KEY` = 你的 Groq Key
- `ZHIPU_API_KEY` = 你的智谱 Key
- `ADMIN_PASSWORD` = 你自己设置的后台密码

建议额外添加：

- `AI_BALANCE_MODE` = `priority`
- `AI_PROVIDER_ORDER` = `groq,zhipu,cloudflare,hfpublic,gemini,custom`
- `HF_PUBLIC_ENABLED` = `true`

如果还要用 Gemini，再添加：

- `GEMINI_API_KEY` = 你的 Google AI Studio Key

### Workers AI Binding

进入 Pages 项目的 Bindings，新增 **Workers AI**，变量名填写：

`AI`

默认模型是：

`@cf/zai-org/glm-4.7-flash`

它不是“公共免账号接口”，但对你自己的 Cloudflare Pages Function 来说不需要再保存第三方 API Key。

## 关于免 Key 公共端点

当前代码内置：

- Base URL：`https://pnywsahxhac1qjbo.us-east-2.aws.endpoints.huggingface.cloud/v1`
- Model：`Qwen/Qwen3.8-Flash-Next`
- Key：任意字符串即可，代码内部使用 `none`

该服务是共享社区端点，不是永久免费 SLA。为了避免它下线后拖慢整站，代码给它设置了较短超时，并且失败后会继续切换下一路。

如果以后它退役，只需设置：

`HF_PUBLIC_ENABLED=false`

无需改代码。

## 图片问卜路由

图片只会发给明确支持 `image_url` 的线路：

1. Groq Qwen
2. HF 公共 Qwen3.8-Flash-Next
3. Gemini（若已配置）

智谱 GLM-4-Flash 和当前 Workers AI 文本模型不会收到图片，从而避免格式错误。

## 密钥安全

不要把真实 Key 写进 `index.html`、`script.js`、`wrangler.toml` 或 GitHub 仓库。这个压缩包也没有写入你提供的真实密钥；实际密钥只应放在 Cloudflare Secret / Environment Variable 中。
