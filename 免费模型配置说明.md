# 推演线路说明

前台只请求本站 `/api/chat`。浏览器看不到供应商和模型代号。

服务端顺序（对用户不可见）：

1. Cloudflare Workers AI（账号内较强文本模型，额度用尽或失败则换下一颗）
2. Groq
3. 智谱开放平台（GLM 4.7 Flash）

心流、魔搭、Hugging Face 公共端点、Gemini、自定义上游已从默认路由移除。

密钥只放在 Cloudflare Variables and Secrets，不要写进仓库。
