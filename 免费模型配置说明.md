# 玄机子 v8.1 · 免费模型与容灾配置

## 1. 推荐结构

不要把十几个模型都放在同一个中转 Base URL 下；那样中转一挂，所有模型一起不可用。本版以“供应商”为一级路由，每家独立失败、独立切换。

### 文本默认顺序

`zhipu → groq → cloudflare → gemini → hfpublic → custom`

- `zhipu`：`glm-4.7-flash`，中文文本主力。
- `groq`：`qwen/qwen3.6-27b`，文本与图片。
- `cloudflare`：`@cf/zai-org/glm-4.7-flash`，通过 Pages/Workers AI Binding 使用，不需要额外第三方 API Key。
- `gemini`：配置 `GEMINI_API_KEY` 后启用，文本与图片备用。
- `hfpublic`：无需注册/无需真正 API Key 的共享公共端点，只作临时备用。
- `custom`：兼容你已有的 OpenAI-compatible 中转。

### 图片默认顺序

`groq → gemini → hfpublic → custom`

智谱 `glm-4.7-flash` 与当前 Cloudflare `glm-4.7-flash` 都按文本线路处理，不会误收图片。

## 2. Cloudflare Secrets

建议在 Cloudflare Dashboard 中配置，而不是写进代码：

```text
GROQ_API_KEY=重新生成后的 Groq Key
ZHIPU_API_KEY=重新生成后的智谱 Key
ADMIN_PASSWORD=自定义后台口令
AI_PROVIDER_ORDER=zhipu,groq,cloudflare,gemini,hfpublic,custom
AI_VISION_PROVIDER_ORDER=groq,gemini,hfpublic,custom
HF_PUBLIC_ENABLED=true
```

可选：

```text
GEMINI_API_KEY=
GROQ_MODEL=qwen/qwen3.6-27b
ZHIPU_MODEL=glm-4.7-flash
CF_AI_MODEL=@cf/zai-org/glm-4.7-flash
HF_PUBLIC_BASE_URL=
HF_PUBLIC_MODEL=Qwen/Qwen3.8-Flash-Next
HF_PUBLIC_KEY=none
API_BASE_URL=
API_KEY=
MODEL_TEXT=
MODEL_VISION=
```

## 3. Workers AI Binding

项目使用 `env.AI`。在 Cloudflare Pages/Workers 项目的 Bindings 中添加 Workers AI，变量名：

```text
AI
```

这条线路不需要再申请智谱/Groq 之类的第三方 Key，但仍受 Cloudflare 自己的套餐、额度和计费规则约束。

## 4. 免 Key 公共 Qwen

当前内置的共享端点是 Qwen3.8-Flash-Next。它的特点是无需注册、无需真实 API Key，但会限流，而且属于临时公共资源，不承诺长期存在。

因此代码把它放在官方账号线路之后。若以后端点换地址，只设置：

```text
HF_PUBLIC_BASE_URL=新的 /v1 Base URL
HF_PUBLIC_MODEL=新的模型 ID
```

不需要修改 `chat.js`。

如果失效，直接：

```text
HF_PUBLIC_ENABLED=false
```

## 5. 后台

后台地址：

```text
/admin.html
```

必须配置：

```text
ADMIN_PASSWORD=你自己的强口令
```

后台 `/api/health` 需要同一个 `ADMIN_PASSWORD` 请求头，只显示线路“已配置/未配置”，不会返回 Key。

## 6. KV 对话记录

`CHAT_LOGS` 用于保存问卜记录，默认保存 90 天。后台会分页读取 KV key，但为了避免一次加载过重，单次最多读取最近 250 条内容。

## 7. 密钥安全

真实 Key 不应出现在：

- `index.html`
- `script.js`
- `functions/api/*.js`
- `wrangler.toml`
- GitHub 仓库

真实 Key 只放 Cloudflare Variables and Secrets 或本机未提交的 `.dev.vars`。

如果 Key 曾经直接发送到聊天、群聊、Issue 或提交记录中，应将该 Key 视为已经暴露，重新生成后再正式部署。
